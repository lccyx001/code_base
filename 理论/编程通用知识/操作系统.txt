1.Linux系统开机流程：

（1）bios，开机主动执行，识别可开机的设备
（2）MBR  第一个可开机设备的第一个扇区内的主引导分区块，内包含引导加载程序
硬件本身会支持
（3）引导加载程序,boot loader 可读取内核文件来执行的软件
该步骤提供3个功能，提供菜单，载入内核文件，转交其他loader(多操作系统的启动会用到)

（4）开始操作系统的功能


2.挂载
利用一个目录当成进入点，将磁盘分区的数据放置在该目录下，也就是说，进入该目录就可以读取该分区的数据
